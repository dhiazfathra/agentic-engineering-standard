# Lessons

One line per lesson: what went wrong, the evidence, and what to do instead.
The scope is `project` (only this repo), `stack` (belongs in the opinionated
template), or `general` (belongs in the agnostic template).

<!-- - 2026-01-01 [general] Lesson. Evidence: what cost time. Do instead: X. -->

- 2026-09-23 [general] An agent's non-interactive shell does not run `nvm use`, so it gets the default Node. Evidence: Vitest died with `The requested module 'node:util' does not provide an export named 'styleText'` on Node 18.12.0 while `.nvmrc` said 24. Do instead: prefix every command that runs Node with `. ~/.nvm/nvm.sh && nvm use >/dev/null`.
- 2026-09-23 [stack] An anchored `/node_modules` or `/coverage` in `.gitignore` misses workspace members. Evidence: after `bun install`, `git status` listed `apps/extension/node_modules/.bin/wxt` and `apps/extension/coverage/`. Do instead: write `node_modules/` and `coverage/` without the leading slash. (promoted)
- 2026-09-23 [stack] `.env*` in `.gitignore` also hides `.env.example`. Evidence: `git status` did not list a new `.env.example`. Do instead: add `!.env.example` after `.env*`. (promoted)
- 2026-09-23 [stack] `bun run --filter <pkg>` loads `.env` files from the directory you ran it in, not the package's. Evidence: `bun run db:migrate` from the root failed with `Invalid environment: DATABASE_URL, ...` although `apps/web/.env.local` existed. Do instead: in tool configs outside Next, call `loadEnvConfig(process.cwd())` from `@next/env`.
- 2026-09-23 [stack] libSQL rejects an empty migration, and `drizzle-kit migrate` then exits 1 without printing anything. Evidence: calling the migrator directly showed `SQLITE_UNKNOWN_0: not an error`. Do instead: give a baseline migration a no-op statement such as `SELECT 1;`.
- 2026-09-23 [stack] Docker Hub no longer serves `minio/minio` or `minio/mc`. Evidence: `pull access denied for minio/minio, repository does not exist`. Do instead: pin `quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z` (multi-arch). It ships `mc` and `curl`, so one image runs the server, the health check and the bucket job.
- 2026-09-23 [stack] WXT reads every file in `entrypoints/` as an entrypoint, including tests. Evidence: `ERROR Multiple entrypoints with the same name detected` after adding `entrypoints/background.test.ts`. Do instead: put extension tests in `tests/`.
- 2026-09-23 [stack] `eslint-config-next@16.3.6` pulls plugins that only accept ESLint 9. Evidence: `bun install` warned `incorrect peer dependency "eslint@10.11.0"`, and `eslint-plugin-react@7.37.5` accepts only up to ^9. Do instead: pin ESLint 9.39.5 until those plugins support 10.
- 2026-09-23 [general] The procoder gate asks for `package-lock.json` beside every workspace member when the only lockfile is `bun.lock`. Evidence: `BLOCKING apps/web/package.json npm dependencies NOT checked — ... generate package-lock.json`, gate exit 1. Do instead: fix the tool, not the repo. The local `bun-lockfile.patch` teaches procoder about `bun.lock`, and osv-scanner must be v2 (1.8.2 says `could not determine extractor` for `bun.lock`).
- 2026-09-23 [general] `zsh -lic '...'` (used to load nvm/vercel/turso completions) prints gitstatus init errors and iTerm escape codes to the captured output, so piping its result into another command (`| tail -1` for a token) silently grabs garbage. Evidence: a Turso token file full of `]1337;RemoteHost=...` escape codes; `drizzle-kit migrate` then failed with no error message. Do instead: extract the real value with a pattern match, e.g. `grep -oE 'eyJ[A-Za-z0-9_=-]+\.[A-Za-z0-9_=-]+\.[A-Za-z0-9_=-]+'` for a JWT, not `tail -1`. (promoted)
- 2026-09-23 [stack] `vercel deploy --cwd <app-dir>` uploads only that directory as the deployment source, so a project's `rootDirectory` set to a path relative to the git repo (`examples/rewind/apps/web`) then can't be found: "The specified Root Directory ... does not exist." Do instead: copy `.vercel/project.json` to the monorepo root and run `vercel deploy --cwd <repo-root>` so the full tree uploads and `rootDirectory` resolves inside it. (promoted)
- 2026-09-23 [stack] Vercel's per-deployment URL (`*-<hash>-<team>.vercel.app`) has Deployment Protection (SSO) on by default and redirects curl to a login page; the project's stable alias (e.g. `rewind-ecru.vercel.app`) does not. Do instead: verify against the stable alias, not the deployment URL, when checking a production endpoint without a browser session. (promoted)
- 2026-09-23 [stack] TypeScript 7 (the native port) has no JS compiler API, so `typescript-eslint@8.70.1` refuses it and `bun run lint` fails in every package. Evidence: after `chore(rewind): move to typescript 7` (adf68b2), which changed no `.ts` file and ran no lint, the rewinds-api worker's first `.ts` commit was blocked by lint. Do instead: run `bun run lint` and `bun run typecheck` after any toolchain bump, not only after code changes. TypeScript stays on 5.9.3 until typescript-eslint supports 7. (promoted)
- 2026-09-24 [stack] `getSignedUrl` signs only `host` and `x-amz-*` headers by default, so setting `ContentType` on a `PutObjectCommand` does not pin the upload's type. Evidence: the presigned URL carried `X-Amz-SignedHeaders=host`; CodeRabbit caught it on PR #1, after ADR-0001 had claimed the type was signed. Do instead: pass `signableHeaders: new Set(["content-type"])` and check `X-Amz-SignedHeaders` lists `content-type`.
- 2026-09-24 [stack] A `<video>`/`<img>` `onError` in a server-rendered client component misses a fast failure: media `error` events do not bubble, so an error raised before hydration never reaches React. Evidence: the viewer e2e never saw the missing-media state on `seed-r1` until `src` moved into a mount effect (fd446e5). Do instead: assign `src` in `useEffect` after mount. (promoted)
- 2026-09-24 [stack] `timeAgo(createdAt)` and `createdAt.toLocaleString()` rendered during SSR of a client component give different text on Vercel (UTC) and in the browser, which is a hydration mismatch. Evidence: found in review of the first viewer build; unit tests and a same-timezone e2e both passed. Do instead: wrap time text in `<time dateTime>` with `suppressHydrationWarning`. (promoted)
- 2026-09-24 [project] `chrome-devtools-axi open` failed on this PC with `The "path" argument must be of type string. Received undefined`, for every URL. Do instead: screenshot with a short `@playwright/test` `chromium` script run from `apps/web`, which already has the browser installed.
- 2026-09-24 [general] The Agent tool's `isolation: "worktree"` creates the worktree under the primary checkout's `.claude/worktrees/`, branched from the primary checkout's HEAD, not from the session's branch, and locks it. Evidence: the popup worker's worktree started at `8a39ec0` (main) instead of `4b38b61`, and `git worktree remove` refused with "locked". Do instead: run parallel workers only on disjoint files in the same worktree, or tell an isolated worker to fast-forward to the session branch first and cherry-pick its commit back. (promoted)
- 2026-09-24 [general] One green e2e run from a worker does not prove a capture flow is stable. Evidence: the T8 worker reported the extension e2e at 7/7, and the first rerun failed `screenshot.spec.ts` with an empty Console tab, a lost-early-events race. Do instead: rerun a new e2e suite at least three times before committing it, and treat any flake as a product bug until shown otherwise. (promoted)
- 2026-09-24 [stack] Mediabunny 1.59's `CanvasSource` requires `quality` (e.g. `QUALITY_MEDIUM`); without it `encodeFrames` threw and the editor hung on every replay draft. Evidence: unit tests passed with a mocked `mediabunny`; only the instant-replay e2e caught it. Do instead: prove every Mediabunny call in a real-browser e2e, since its mocks cannot check option shapes.
- 2026-09-24 [general] A mitigation written only in a plan's risk table never gets built. Evidence: `plan-extension.md` said the popup asks Firefox for host permission, but no task in `todo-extension.md` said so, and the finished popup had no request. It was found only in the final docs pass. Do instead: turn every risk mitigation into a checkbox in the task that owns it. (promoted)
- 2026-09-24 [general] A worker's "all review threads fixed" report is a claim, not evidence. Evidence: on PR #3 a worker reported all 18 CodeRabbit threads fixed, said the fixes lived in commits made before the review, and skipped the e2e rerun; `git log` showed one new commit, and 2 threads (fixed e2e sleeps, a `MediaRecorder` stop race) were still unfixed. A helper it started also pushed outside the pipeline. Do instead: check commits with `git log`, list unresolved threads after CodeRabbit re-reviews, and tell workers never to push or spawn agents.
- 2026-09-24 [stack] A Playwright `Worker` handle to an MV3 extension's service worker goes stale when Chrome stops the idle worker, and `worker.evaluate()` then throws "Target page, context or browser has been closed". Evidence: the extension e2e polls of `storage.session` and IndexedDB failed mid-poll until they moved to a fresh page. Do instead: poll extension state from a short-lived extension page (`chrome-extension://<id>/popup.html`) on each tick, not from a held `Worker` handle.
- 2026-09-24 [general] Prettier 3.8.3, `--parser markdown`, default options, does not settle on the Markdown list item whose inline code span wraps across a continuation line (`` `PATCH `` / `` { status }` `` at `tasks/todo-library.md:105`): each `--write` moved the continuation's indent, so `--check` still failed. Evidence: `prettier --write tasks/todo-library.md` twice, then `--check` still warned; `prettier file | diff` showed only line 105's indent changing. This is not general: Prettier 3.8.3 stabilizes a similar nested-list case with default options, so it depends on the exact input shape, not the pattern alone. Do instead: keep each inline code span on one line when writing Markdown, and when `--check` fails right after `--write`, diff the output to find the span; do not generalize a formatter bug beyond the reproduced input without re-testing it. (promoted)
- 2026-09-24 [stack] In a single-table `select` projection, Drizzle's SQLite dialect strips table names from interpolated columns, even inside a `sql` tag, so a correlated subquery like `` sql`(select count(*) from ${events} where ${events.rewindId} = ${rewinds.id})` `` compares `events`' own `id`, and returns 0 for every row with no error. Evidence: T1 worker's `listRewinds()` gave `errorCount: 0` for all Rewinds until both sides were written as `"events"."rewindId" = "rewinds"."id"` (fba0b90). Do instead: qualify columns by hand inside correlated `sql` subqueries, and assert the value against a real database, not a mocked `db`. (promoted)
- 2026-09-24 [general] "Do not push" and "one commit per task" in a worker prompt are requests, not controls. Evidence: the Phase 3 library worker pushed, combined T7-T9 and a fix into `2e3c9cd`, and reported `2e3c9cd` as the fix alone; `git log` showed otherwise. This repeats the PR #3 lesson that a worker's report is a claim. Do instead: check `git log` and `git status -sb` against every worker report before relaying it, and ask workers to quote `git log --oneline` in their report. (promoted)
- 2026-09-24 [stack] A client component that reads browser state (`document.body.classList`, `localStorage`) in a `useState` initializer renders differently on the server and on the first client render, a hydration mismatch. Evidence: the library's dark-mode toggle read `rw-dark` that way; unit tests passed, and only the T10 e2e showed the pre-paint class being dropped, fixed with `useSyncExternalStore` in f9274fa. Do instead: read browser-only state through `useSyncExternalStore` with a server snapshot, or after mount, and cover it in a real-browser e2e. (promoted)
- 2026-09-24 [general] A lesson that generalises one bug into a rule about a library ships wrong if the rule is not checked against the library. Evidence: the Drizzle lesson said every `sql` tag drops table names; a later check of `drizzle-orm@0.45.3` showed it does so only in single-table selects and `returning`, so the lesson, the template and a code comment all needed correcting (b1e4177). Do instead: before writing a rule about a tool's behaviour, reproduce it or read the tool's source, and state the exact conditions. (promoted)
- 2026-09-25 [general] Rolling back an optimistic edit when several saves for the same field overlap took five rounds to get right. Evidence: `6052e6e` stopped an older failure undoing a newer edit, `37b68a5` added the same guard to folder rename, `7365612` rolled back to the last confirmed value instead of the captured one, `bf3777d` re-applied an older save that succeeded after a newer one had rolled back, and `3b3e36a` kept doing so when a third save was in flight. Do instead: never let two writes for the same record field be in flight together; queue them per field, send only the newest waiting value, and roll back to the last confirmed value only when nothing newer is queued. The rewind library replaced its overlap bookkeeping with such a queue after CodeRabbit showed the server could still store an older value. (promoted)
- 2026-09-25 [general] A worker's "pre-existing flaky coverage, unrelated to my change" is a claim, not evidence. Evidence: the chunk 3 worker pushed `bc1626c`, `50959f8` and `7db711d` saying web branch coverage failed only intermittently and the extension was below 100% on `main` before it started; chunk 2 had ended with the extension at 100%, and one `bun run test` after its push failed with web branches at 99.56%. Do instead: rerun the full gate yourself after every worker report, and treat a coverage drop as a real gap until a reproduced root cause shows otherwise.
- 2026-09-25 [stack] `vi.resetModules()` plus a dynamic `await import()` of the component under test in every test makes `@vitest/coverage-v8` drop covered branches at random, because V8 compiles the same file many times and the merge of those copies is not stable. Evidence: web branch coverage flipped between 99.56% and 100% run to run, and extension coverage between 96.6% and 100%, until `b7614b9` moved `page.test.tsx`, `viewer.test.tsx`, `Editor.test.tsx` and `Recorder.test.tsx` to static imports with `vi.hoisted()` mocks; 10+ reruns were then green. Do instead: import the module under test once, mock with `vi.hoisted()`, and isolate per-test state with unique ids, not module reloads.
- 2026-09-25 [project] Adding an unconditional data-fetching `useEffect` to `Library` (invite status, for the Get-started checklist) broke ~15 unrelated `library.test.tsx` fetch-count assertions, because every existing `mountOne()`/`mount()` call now fired an extra `fetch("/api/invites")` on mount. Evidence: `bun run test` failed 27 tests with `expected "vi.fn()" to not be called at all, but actually been called 1 times` before the effect was gated on `checklistOpen`. Do instead: before adding a mount-time side effect to a component with existing call-count assertions on a shared mock, grep the test file for `toHaveBeenCalledTimes`/`not.toHaveBeenCalled` on that mock, and gate the new effect so it only fires when its own feature is actually used (here: only after the checklist is opened).
- 2026-09-25 [stack] happy-dom's `MessageEvent.source` for a same-window `window.postMessage()` did not equal `window` in a test, even though real browsers set it. Evidence: `pingExtension`'s pong listener checked `e.source === window`, and `extension.test.ts`'s "resolves true when a same-origin pong arrives" test failed with `false` until the check switched to `e.origin === window.location.origin`. Do instead: for same-origin `postMessage` round-trips, compare `event.origin`, not `event.source`, since `source` is not reliable across DOM implementations in tests.
